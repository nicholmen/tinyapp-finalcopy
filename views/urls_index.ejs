<!DOCTYPE html>
<html lang="en">
    <head>
        <title>TinyApp</title>
    </head>
    <header>
        <% include ./partials/_header %>
    </header>
    <body>
        <ul><% for(var shortURL in urls){ %>
            <li>
                <form method="POST" action="/urls/<%= shortURL %>/delete">
                <%= shortURL %> -> <%= urls[shortURL].longURL %> <a href = "/urls/<%= shortURL %>">Edit</a>
                <input type="submit" value="Delete">
                </form>
            </li>
        <% } %>
        </ul>
        <p><a href = '/urls/new'>shorten please, not the bees</a><p>
    </body>
</html>