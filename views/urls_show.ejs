<!DOCTYPE html>
<html lang="en">
    <head>
        <title>TinyApp</title>
    </head>
    <header>
        <% include ./partials/_header %>
    </header>
    <body>
        <p>Short URL: <a href='/u/<%= shortURL %>'>http://www.localhost:8080/u/<%= shortURL %></a></p>
        <form action="/urls/<%= shortURL %>/edit" method="POST">
            <p>Edit your link:</p>
        <input id="longURL" type="text" name="longURL" placeholder="<%= longURL %>" style="width: 300px">
        <input type="submit" value="Update">
    </form>
        
    <a href = '/urls'>Back to URLs</a>
    </body>
</html>